package gmit.ie;

import java.io.*;

public class FileWriter {
	public void writer(String fileName, String text) throws Exception {
		OutputStreamWriter writer = new OutputStreamWriter(new FileWriter(fileName));
		writer.write(text);
		writer.close();
	}
}
